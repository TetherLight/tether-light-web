# Tether Light コーポレートサイト — STUDIO再現用 設計書

対象コード: `index.html` / `css/style.css` / `js/script.js` / `news/*.html`
このドキュメントは、既存のHTML/CSSを解析し、STUDIO（ノーコードエディタ）の操作用語に翻訳したものです。数値はすべてコードの実測値です。推測を含む箇所には【要確認】を付けています。

> 【要確認・全体前提】本書ではSTUDIOの標準ブレークポイントを **PC=1440px／タブレット=768px／SP(モバイル)=375px** と仮定してフォントサイズ等を算出しています。実際のSTUDIOプロジェクトのブレークポイント幅がこれと異なる場合、タブレット・SPの数値は再計算が必要です。エディタ右上の「PC/タブレット/SP」切り替え時の基準幅を確認してください。
>
> 元のコードのブレークポイントは `1024px` `860px` `480px` の3段階で、STUDIOの3段階（1440/768/375）とは境界が一致しません。本書では「768pxのとき元コードのどの分岐が適用されるか」を計算した上で数値化しています（768pxは元コードの`max-width:860px`の分岐に入り、`max-width:480px`には入りません。375pxは両方の分岐に入ります）。

---

## 1. デザイントークン一覧

### 1-1. カラー

| トークン名（コード上の変数） | HEX | 用途 | 使用箇所 |
|---|---|---|---|
| `--color-primary` | `#649CD3` | アクセントカラー（水色） | ロゴ、日付テキスト、タグ文字、ボーダー装飾、ホバー時の文字色、CTA/ヒーローのグラデーション末端 |
| `--color-primary-dark` | `#4B759E` | プライマリの濃色版 | 会社概要テーブルの見出しセル文字色、タグの文字色、ニュース「VIEW ALL」の文字色、ホバー背景 |
| `--color-primary-light` | `#83B0DC` | プライマリの明色版 | **定義されているが現在CSS内で未使用**（将来利用を想定した予備トークンと判断） |
| `--color-primary-tint` | `#F3F7FB` | プライマリの極薄background用 | タグの背景色、会社概要テーブルの見出しセル背景 |
| `--color-white` | `#FFFFFF` | 白 | 本文背景、ヒーロー内テキスト色、ボタン背景 |
| `--color-black` | `#1A1B24` | 本文用の黒（純黒ではない） | 本文テキスト色、見出し色 |
| `--color-gray` | `#6B7180` | 補助テキスト（グレー） | リード文、キャプション、フッターリンク |
| `--color-gray-light` | `#F5F6FA` | 薄いグレー背景 | 事業内容セクションの背景、ニュース行のホバー背景 |
| `--color-border` | `#E3E6F0` | ボーダー線 | セクション区切り線、テーブル枠線、ニュース行の下線 |

**変数化されていない直接指定のHEX（コード内にそのまま書かれている値）**

| HEX | 用途 | 備考 |
|---|---|---|
| `#05060B` | ヒーロー背景グラデーションの開始色（ほぼ黒） | 3色グラデーションの1色目 |
| `#12162B` | ヒーロー背景グラデーションの中間色（濃紺） | 2色目（45%地点） |
| `#263465` | ヒーロー背景グラデーションの中間色2（紺） | 3色目（80%地点、4色目は`--color-primary`） |
| `#5E76C7` | 事業内容「撮影事業」画像枠のグラデーション開始色 | `--color-primary`との2色グラデーション |
| `#2C3B75` | 事業内容「SNS運営事業」画像枠、CTA背景のグラデーション色 | 2箇所で使用 |

**【要確認】色の不整合**: ヒーロー背景の装飾用radial-gradient（2箇所）は `rgba(70, 93, 170, ...)` という色を使っています。これは現在の`--color-primary`（#649CD3 = rgb(100,156,211)）ではなく、**旧配色（#465DAA）の色のまま**残っている値です。意図的な演出でなければ、STUDIO側では`--color-primary`と同じ`#649CD3`に統一することを推奨します。再現するだけであれば `rgb(70, 93, 170)` をそのまま使ってください。

**透明度つきの色（rgba）— 主なもの**

| 用途 | 値 |
|---|---|
| ヘッダーのガラス風背景 | `rgba(255,255,255,0.92)` |
| ヒーロー文中のサブテキスト | `rgba(255,255,255,0.7)` |
| ヒーロー内の飾り罫線 | `rgba(255,255,255,0.4)` |
| ボタンホバーの影 | `rgba(0,0,0,0.15)` |

---

### 1-2. フォント

| 役割 | フォント名 | ウェイト | 提供元 |
|---|---|---|---|
| 日本語本文 (`--font-jp`) | Noto Sans JP | 300 / 400 / 500 / 700 / 900 を読み込み（実使用は400・500・700） | Google Fonts |
| 英数字・ラテン文字 (`--font-en`) | Inter Tight | 300–800を読み込み（実使用は500・600・700） | Google Fonts |
| 手書き風見出し (`--font-hand`) | **Harenosora**（カスタムフォント、独自入手） | 400のみ | **Google Fontsではない。`fonts/Harenosora.otf`（4.15MB、OTF形式）として自前で読み込んでいる** |
| 手書き風の代替フォント | Klee One | 400 / 600 | Google Fonts（Harenosoraが使えない場合のフォールバックとして指定） |

フォント適用の優先順位（フォールバックチェーン）:
- 日本語本文: `Noto Sans JP → ヒラギノ角ゴ →游ゴシック → 標準ゴシック`
- 英数字: `Inter Tight → Noto Sans JP → 標準ゴシック`
- 手書き見出し: `Harenosora → Klee One → Noto Sans JP → 標準の手書き体`

> 【要確認】STUDIOでカスタムフォント（.otf/.ttfのアップロード）に対応しているプランかどうかを確認してください。対応していない場合は、手書き見出し（トップの「写真の未来を繋ぐ。」）はGoogle Fontsの **Klee One（セミボルド600）** で代替してください。

---

### 1-3. タイポグラフィ（フォントサイズ・行間・字間）

行間はコード上は単位なしの数値（例: `1.15`）です。ご要望に合わせて **%表記＝数値×100** で併記します。字間は`em`指定のため、**%表記＝em値×100** としてそのまま換算できます（1em = フォントサイズの100%）。

PC・タブレット・SPで数値が変わる項目は、CSSの `clamp(最小値, 可変値, 最大値)` という「画面幅に応じて自動で線形補間するが上下限がある」指定によるものです。STUDIOには`clamp()`に相当する機能がないため、**3段階のブレークポイントごとに固定pxを設定する**必要があります。以下は解析済みの確定値です。

| 要素 | フォント | PC (1440px) | タブレット (768px) | SP (375px) | ウェイト | 行間 | 字間 |
|---|---|---|---|---|---|---|---|
| ページタイトル (`.section-title` = 「事業内容」「ニュース」「会社概要」の見出し) | 日本語 | 40px | 31px | 28px | 700 | 180%（body継承、明示指定なし） | 2% |
| ヒーロー見出し「写真の未来を繋ぐ。」 (`.hero__tagline`) | 手書き(Harenosora) | 76px | 45px | 36px | 400 | 115% | 1% |
| ヒーロー説明文 (`.hero__lead`) | 日本語 | 17px | 15px | 15px | 400（明示指定なし） | 180%（body継承） | 0 |
| 事業内容の項目タイトル「撮影事業」等 (`.business-item__title`) | 日本語 | 28px | 23px | 22px | 700 | 180%（body継承） | 0 |
| CTA見出し「まずはお気軽に〜」 (`.cta__title`) | 日本語 | 32px | 26px | 24px | 700 | 180%（body継承） | 0 |
| ニュース詳細記事タイトル (`.article__title`) | 日本語 | 34px | 28px | 24px | 700 | 150% | 0 |

上記以外（固定サイズ・画面幅で変わらない項目）:

| 要素 | フォント | サイズ(PC=タブレット共通) | SPでの変化 | ウェイト | 字間 |
|---|---|---|---|---|---|
| セクション上の小ラベル（"OUR BUSINESS"等） (`.section-tag`) | 英数字 | 14px | 変化なし | 700 | 15% |
| ヒーロー冒頭の英字キャッチ (`.hero__eyebrow`) | 英数字 | 13px | 変化なし | 500 | 20% |
| ナビゲーションリンク (`.nav__link`) | 日本語 | 15px | 変化なし | 500 | 0 |
| ボタン文字 (`.btn`) | 日本語 | 15px | 変化なし | 500 | 3% |
| ニュース一覧の日付 (`.news-item__date`) | 英数字 | 14px | タブレット以下13px | 600 | 4% |
| ニュース一覧のタイトル文字 (`.news-item__title`) | 日本語 | 15.5px | タブレット以下14.5px | 400（明示指定なし） | 0 |
| 会社概要テーブルの文字 (`.company-table th/td`) | 日本語 | 14.5px | SP(≤480px)のみ13.5px | th=700 / td=400 | 0 |
| フッターのリンク文字 (`.footer__nav a` `.footer__sns a`) | 日本語 | 13.5px | 変化なし | 400 | 0 |
| フッターの著作権表記 (`.footer__copy`) | 日本語 | 11.5px | 変化なし | 400 | 0 |
| 記事本文 (`.article__body`) | 日本語 | 15.5px | 変化なし | 400 | 0 |
| 記事の日付表示・戻るリンク (`.article__date` `.article__back`) | 英数字 | 14px / 13px | 変化なし | 600 | 4% |

**行間について【重要な事実】**: 上表で「180%（body継承）」と書いた要素は、CSS側で行間を個別指定していません。そのため`<body>`に設定された **180%（1.8）** がそのまま適用されています。見出し文字にしては行間が広いという印象を持たれるかもしれませんが、これはコードの実際の挙動であり、誤植ではありません。STUDIOで再現する際は、見出しに180%の行間を設定すると元と同じ見た目になります（一般的な見出しの行間130〜140%より広い点に注意してください）。

---

### 1-4. 余白（マージン・パディング・gap）

**セクションの上下余白**（`.section`クラスが付いた大枠。事業内容・ニュース・会社概要の3セクションに共通）

| ブレークポイント | 値 |
|---|---|
| PC / タブレット上位（〜1024px超） | 100px |
| 〜860px | 72px |
| 〜480px | 56px |

**コンテナの左右内側余白**（`.container`。ページ全体の左右マージンに相当）

| ブレークポイント | 最大幅 | 左右パディング |
|---|---|---|
| 〜480px超 | 1160px（中央寄せ） | 24px |
| 〜480px以下 | 1160px | 20px |

**要素間の余白（gap/margin）— コード内での使用頻度順**

| 値 | 出現回数 | 主な使用箇所（例） |
|---|---|---|
| 16px | 6回 | ヒーロー小ロゴ下margin、ボタン内パディング、CTA見出し下margin、タグのパディング 等 |
| 10px | 5回 | ボタン内アイコンgap、ロゴのアイコン文字間gap、ナビ「お問い合わせ」ボタンの縦パディング、事業内容タグリストのgap 等 |
| 24px | 4回 | 説明文の下margin、記事本文の段落間、フッター内の各要素間gap |
| 28px | 4回 | ヒーロー英字キャッチの下margin、ニュース行同士のgap、会社概要セルの横パディング、フッターの上下パディング |
| 40px | 4回 | ナビリンク同士のgap、ニュース一覧全体の下margin、記事の「戻る」リンク下margin、記事タイトル下margin |
| 8px | 3回 | ニュース「VIEW ALL」内gap、タグのパディング、記事「戻る」リンク内gap |
| 20px | 2回 | ページ見出し下margin、フッター著作権の上margin |
| 22px | 2回 | ナビボタンの横パディング、会社概要セルの縦パディング |
| 44px | 2回 | ヒーロー見出し下margin、ヒーロー説明文下margin |
| その他（1回のみ） | — | 4px・5px・26px・32px・36px・56px・72px・90px・100px |

---

## 2. セクション別レイアウト仕様

ページ構成順: **ヘッダー → ヒーロー → 事業内容 → ニュース → 会社概要 → お問い合わせCTA → フッター**

各セクションの「ボックス構造」はSTUDIOの「ボックスを入れ子にする」考え方に対応させています。「グループ」はSTUDIOの複数要素をまとめる操作に相当します。

### 2-1. ヘッダー（画面上部に固定）

- ヘッダー全体（固定表示・幅100%・高さ76px、SPでは64px）
  - コンテナ（横並び、左右に配置＝両端揃え、最大幅1160px、中央寄せ）
    - ロゴ グループ（横並び、要素間gap 10px）
      - アイコン画像（白版）／アイコン画像（青版）※詳細は3章
      - テキストロゴ画像（白版）／テキストロゴ画像（青版）※詳細は3章
    - ナビゲーション グループ（横並び、要素間gap 40px）
      - 「事業内容」リンク
      - 「ニュース」リンク
      - 「会社概要」リンク
      - 「お問い合わせ」リンク（背景色付きの丸ボタン型。パディング上下10px・左右22px、角丸999px＝完全な丸角）
    - ハンバーガーボタン（PC/タブレットでは非表示。860px以下でのみ表示）

**配色の切り替え挙動（要素の見た目がスクロールで変わる）**:
- ページ最上部（ヒーロー上、背景が透明）: ロゴは白、文字は白
- 少しスクロールすると: 背景が白半透明（`rgba(255,255,255,0.92)`）＋ぼかし効果に切り替わり、ロゴ・文字は黒に切り替わる
- ニュース詳細ページ等（ヒーローがないページ）: 常に白背景・黒文字の状態で固定表示

**レスポンシブでの変化（860px以下）**:
- ナビゲーション部分がハンバーガーボタンに切り替わり、タップすると画面右からナビが白背景でスライドイン（横並び→縦積みリストに変化、パディング32px 24px）

---

### 2-2. ヒーロー（トップの大きな画面。高さ=画面いっぱい`100vh`）

- ヒーロー全体（高さ:画面の高さ100%、背景はダーク系グラデーション、詳細は3章）
  - コンテナ（縦積み、上部に画面上部からの余白として76px相当を確保）
    - 英字キャッチ「TETHERING THE FUTURE OF PHOTOGRAPHY」
    - 見出し「写真の未来を繋ぐ。」（手書きフォント、下に手描き風の下線装飾＝3章で詳細）
    - 小ロゴ画像（テキストロゴ白版、幅は画面幅に応じて140〜200pxで可変）
    - 説明文2行（改行はPC表示時のみ入る／SPでは自然に折り返す）
  - スクロール誘導リンク（画面下部中央に絶対配置、「SCROLL」の文字＋下に伸びる縦線。縦線内を小さな棒が上から下へ流れ続けるアニメーション＝3章）

**幅指定**: 説明文（`.hero__lead`）のみ最大幅560pxで折り返し制限。他の要素は幅指定なし（コンテナ幅に依存）。

---

### 2-3. 事業内容セクション

- セクション（背景：薄いグレー `#F5F6FA`）
  - コンテナ
    - 小ラベル「OUR BUSINESS」
    - 見出し「事業内容」
    - リード文「撮影とメディア運営、2つの領域を〜」（最大幅560px、中央寄せ）
    - 項目リスト（縦積み、項目間gap 72px）
      - 項目1「撮影事業」（**横並び2列**、列間gap 56px、縦中央揃え）
        - 左列: 画像枠（グラデーション背景＋絵文字📷、比率4:3固定）
        - 右列: ラベル「SERVICE 01」／タイトル／説明文／タグ4つ（横並び・折り返し可、gap 10px）
      - 項目2「SNSメディア運営事業」（**同じく横並び2列だが左右が反転**＝画像が右、テキストが左）

**幅指定**: 画像枠・テキスト列とも `1fr`（＝2等分の可変幅）。コンテナ最大幅1160pxから左右パディングとgapを引いた残りを2等分するため、実測でおおよそ**1列あたり552px前後**（画面幅により変動）。

**レスポンシブ（860px以下）**: 横並び2列 → **縦1列に変化**。項目2の「画像が右」という反転も解除され、通常順（画像→テキスト）に統一されます。列間gapは28pxに縮小。

---

### 2-4. ニュースセクション

- セクション（背景：白）
  - コンテナ
    - 小ラベル「LATEST NEWS」
    - 見出し「ニュース」
    - ニュース行リスト（縦積み、リスト全体の上に横線ボーダー、下に40pxの余白）
      - 行1〜3（各行は横並び：日付／タイトル／矢印の3要素、要素間gap 28px、各行の下に横線ボーダー、パディング上下26px）
    - 「VIEW ALL NEWS →」リンク（中央寄せ、文字＋矢印アイコンの横並び、gap 8px）

**インタラクション**: 各行はクリック可能な**リンク（`<a>`タグ）で1行全体を覆っている**ため、行のどこをクリックしても遷移します。マウスを乗せると行全体の背景が薄いグレーになり、右端の矢印が右に4pxスライドします。

**リンク先**: 各行・VIEW ALLボタンともに、`news/`フォルダ内の**別々のHTMLページ**に遷移します（4章のアセット一覧を参照）。

**レスポンシブ（860px以下）**: 行内のgapが16pxに縮小、日付列の幅が90px→70pxに縮小。

---

### 2-5. 会社概要セクション

- セクション（背景：白）
  - コンテナ
    - 小ラベル「COMPANY」
    - 見出し「会社概要」
    - テーブル枠（最大幅800px、中央寄せ、角丸20px、枠線1px）
      - **HTML `<table>` 要素**（7行×2列：社名／設立／代表取締役／所在地／資本金／事業内容／取引銀行）
        - 左セル（見出し列、幅160px固定、背景`#F3F7FB`、文字色`#4B759E`太字）
        - 右セル（内容列、幅は残り全部、文字色`#1A1B24`）
        - 各行の下に横線ボーダー（最終行のみ無し）

**レスポンシブ（480px以下）**: セルのパディングが22px 28px→16px 18pxに縮小、見出し列の幅が160px→110pxに縮小、文字サイズが14.5px→13.5pxに縮小。

---

### 2-6. お問い合わせCTAセクション

- セクション（背景：斜めグラデーション、`#2C3B75`→`--color-primary`、パディング上下90px）
  - コンテナ（中央寄せテキスト、文字色白）
    - 見出し「まずはお気軽にご相談ください」
    - 説明文「撮影のご依頼、SNS運用のご相談など〜」
    - ボタン「お問い合わせはこちら →」（白背景・青文字の丸ボタン、`mailto:info@tetherlight.jp`へのリンク）

---

### 2-7. フッター

- フッター全体（背景：白、上に横線ボーダー、パディング上下28px）
  - コンテナ（**横並び、両端揃え、折り返し可、要素間gap 24px**）
    - ロゴ グループ（アイコン画像青版＋テキスト「Tether Light」）
    - フッターナビ（横並びリスト、gap 24px：事業内容／ニュース／会社概要／お問い合わせ）
    - SNSリンク（横並びリスト、gap 24px：Instagram／X／YouTube）※**現在すべて`href="#"`のダミーリンクです。実際のSNS URLに置き換えてください**
  - 著作権表記「© 2026 Tether Light Inc. All Rights Reserved.」（中央寄せ、フッター全体の上margin20px）

**レスポンシブ（860px以下）**: 横並び→縦積み・中央寄せに変化。

---

### 2-8. ニュース詳細ページ・一覧ページ（`news/`フォルダ内、別HTMLファイル）

トップページとは別のファイルですが、ヘッダー・フッターは共通のデザインを再利用しています（STUDIOでは「共通ヘッダー/フッター」機能、またはページごとに複製配置してください）。

- ヘッダー（常に白背景・黒文字で固定表示。トップページと違い、スクロールしても透明にならない仕様）
- 記事本体（コンテナ、最大幅720px、上部パディングは「ヘッダー高さ76px＋64px」分確保）
  - 「← ニュース一覧へ」戻るリンク（下margin40px）
  - 日付テキスト（プライマリカラー、下margin16px）
  - 記事タイトル（見出し、下に横線ボーダー、下margin40px）
  - 本文（段落ごとにmargin-bottom 24px）
- フッター（トップページと同一構成、リンク先のみ`../index.html#各セクション`に変更）

一覧ページ（`news/index.html`）は同じ構造で、記事本体の代わりに「ニュース一覧」見出し＋全記事のリスト（トップページのニュース欄と同じ行デザイン）を表示しています。

---

## 3. STUDIOで再現できない箇所

見落としを避けるため、CSSファイル全体をスキャンして該当箇所を抽出しました。全 **10件** です。

### 3-1. ヘッダーのガラスすり素材効果（`backdrop-filter: blur(10px)`）
- **該当箇所**: スクロール後のヘッダー背景（`.header.is-scrolled` / `.header.header--solid`）
- **なぜ再現困難か**: `backdrop-filter`は「要素の背後にある内容をぼかす」CSS機能で、STUDIOにはこの効果を直接設定する項目がありません。
- **STUDIOでの代替案**: ぼかし効果を諦め、単色の半透明白背景（`rgba(255,255,255,0.92)`）のみで代替してください。見た目の差は「背後がぼやけて見えるかどうか」のみで、色味への影響は小さいです。

### 3-2. ナビリンクのホバー時アンダーライン（`::after`疑似要素）
- **該当箇所**: ヘッダーのナビリンク（「事業内容」等）にマウスを乗せると、下から線が伸びてくる演出
- **なぜ再現困難か**: `::after`疑似要素（要素の直後に見えない子要素を追加するCSS機能）はSTUDIOの要素として存在せず、直接追加できません。
- **STUDIOでの代替案**: 各ナビリンクの下に、幅0・高さ2pxの**実体のBox要素**を重ねて配置し、STUDIOのインタラクション機能で「ホバー時に幅を0%→100%にアニメーション」を設定してください。

### 3-3. ヒーロー背景の格子パターン＋フェード（`mask-image`）
- **該当箇所**: ヒーロー背景に薄く重なっている格子状の線（`.hero__overlay`）。上下端が透明にフェードしている
- **なぜ再現困難か**: `mask-image`（要素の一部を透明にマスクするCSS機能）に相当する機能がSTUDIOにありません。
- **STUDIOでの代替案**: 格子パターンとフェードを**あらかじめ1枚の透過PNG画像として書き出し**、ヒーロー背景の上に重ねて配置してください（4章のアセット一覧に追加が必要な画像として記載）。省略しても全体の印象への影響は小さい装飾です。

### 3-4. 見出し下の手描き風アンダーライン（`::after`＋SVG）
- **該当箇所**: ヒーロー見出し「写真の未来を繋ぐ。」の下にある、白い筆致風の下線（`.hero__tagline::after`）
- **なぜ再現困難か**: `::after`疑似要素に、テキスト幅に応じて伸縮するSVG画像を背景表示する実装のため、STUDIOに存在しない機能を2つ（疑似要素／可変SVG背景）組み合わせています。
- **STUDIOでの代替案**: この下線を**1枚の画像（PNGまたはSVG）として書き出し**、見出しテキストの背面に絶対配置した画像要素として重ねてください。テキストの文字数や画面幅が変わると位置がずれるため、見出しと下線画像をひとつの「グループ」にまとめて一体で動かすようにしてください。

### 3-5. スクロール誘導の流れるアニメーション（`@keyframes`）
- **該当箇所**: ヒーロー下部「SCROLL」の縦線内を、小さな棒が上から下へ流れ続ける演出（`.hero__scroll-line::after` / `@keyframes scrollLine`）
- **なぜ再現困難か**: CSSの`@keyframes`（無限ループする独自アニメーション）を直接設定する機能がSTUDIOにありません。
- **STUDIOでの代替案**: 静止した縦線のみに簡略化するか、STUDIOに用意されている既存のアニメーション効果（フェードやパルスなど）で近い印象のものを探して代用してください。完全な再現は困難です。

### 3-6. 事業内容の「画像とテキストの左右反転」（`order`プロパティ）
- **該当箇所**: 事業内容の2項目目「SNSメディア運営事業」で、画像が右・テキストが左に入れ替わっている配置（`.business-item--reverse` の `order`指定）
- **なぜ再現困難か**: CSSの`order`は「見た目の並び順だけをHTMLの記述順と無関係に入れ替える」機能で、STUDIO上のボックスの並び順はHTML構造の登録順と直結しているため、この「表示だけ入れ替える」操作はできません。
- **STUDIOでの代替案**: 2項目目は最初から「画像ボックスを右側に、テキストボックスを左側に」**配置し直した状態で組んでください**（見た目が同じ結果になれば、内部の順序が異なっていても問題ありません）。

### 3-7. 事業内容の画像枠に重なる光の演出（`::before`＋放射グラデーション）
- **該当箇所**: 事業内容の画像枠（グラデーション背景の四角）に薄く重なっている、2つの光の玉のような装飾（`.business-item__visual::before`）
- **なぜ再現困難か**: `::before`疑似要素で、ベースのグラデーションの上にさらに別の放射グラデーションを重ねています。STUDIOの背景設定は1つのボックスに1種類の塗り（単色／グラデーション／画像）しか設定できません。
- **STUDIOでの代替案**: 同じ位置に**もう1枚、透明度を下げた放射グラデーションのBox要素を重ねて配置**してください（土台のグラデーションBoxの上に、光の玉用のBoxを絶対配置で重ねる2層構成）。

### 3-8. 絵文字アイコンの影（`filter: drop-shadow`）
- **該当箇所**: 事業内容の📷📱アイコンに付いている柔らかい影
- **なぜ再現困難か**: `filter`（要素そのものに画像処理効果をかけるCSS機能）はSTUDIOのテキスト要素・絵文字には設定できません（Box要素の「シャドウ」設定とは別物です）。
- **STUDIOでの代替案**: 絵文字はテキストのまま影なしで使用するか、影付きのアイコンをあらかじめ**画像化（PNG）して書き出し**、画像として配置してください。

### 3-9. カスタムフォントファイルの読み込み（`@font-face`）
- **該当箇所**: 見出し「写真の未来を繋ぐ。」に使っている手書きフォント「Harenosora」（`fonts/Harenosora.otf`）
- **なぜ再現困難か**: Google Fonts以外の独自フォントファイルの読み込みは、STUDIOのプラン・仕様によって対応有無が分かれます。
- **STUDIOでの代替案**: 【要確認】カスタムフォントのアップロードに対応していれば`Harenosora.otf`をそのままアップロードしてください。対応していない場合は、フォールバックとして指定されている **Klee One（セミボルド600、Google Fonts）** に差し替えてください。

### 3-10. 会社概要の表（HTML `<table>`要素）
- **該当箇所**: 会社概要セクションの「社名／設立／代表取締役…」の表組み（`<table>`）
- **なぜ再現困難か**: STUDIOのビジュアルエディタには表（テーブル）専用の要素がなく、`<table>`のような行・列構造をそのまま組む機能がありません。
- **STUDIOでの代替案**: 1行を「見出しBox（幅160px・背景色`#F3F7FB`）＋内容Box（残り幅）」の横並びグループとして作成し、それを7行分**縦に複製**してください。各行の下に1pxの横線ボーダーを追加します。

---

## 4. 書き出しが必要なアセット一覧

### 4-1. 既存の画像ファイル（そのまま使用可能）

| ファイルパス | 実寸 | 表示サイズ | 用途 |
|---|---|---|---|
| `images/logo-icon-white.png` | 349×500px | 表示枠28×28px内に収まる比率で縮小（実寸は約20×28px相当） | ヘッダーロゴのアイコン（白版・透明背景時に表示） |
| `images/logo-icon-blue.png` | 348×500px | 同上 | ヘッダーロゴのアイコン（青版・スクロール後に表示）／フッターロゴのアイコン |
| `images/logo-text-white.png` | 929×172px（横長） | ヘッダーでは高さ15px（幅約81px）／ヒーローでは幅140〜200px可変 | ヘッダーロゴの文字部分（白版）／ヒーロー内の小ロゴ |
| `images/logo-text-blue.png` | 929×172px（横長） | ヘッダーでは高さ15px（幅約81px） | ヘッダーロゴの文字部分（青版） |
| `fonts/Harenosora.otf` | — | — | 手書きフォント本体（3-9参照） |

### 4-2. CSSで描画されているが、STUDIOでは画像化が必要なもの

| 内容 | 現在の実装 | 書き出しが必要な理由 | 推奨仕様 |
|---|---|---|---|
| ヒーロー背景の格子パターン＋上下フェード | `.hero__overlay`（CSSグラデーション＋mask） | `mask-image`が使えないため（3-3参照） | 48px間隔の格子線、白・不透明度6%、上下がフェードする透過PNG |
| 見出し下の手描き風下線 | `.hero__tagline::after`（インラインSVG） | `::after`と可変SVG背景が使えないため（3-4参照） | 白色、先端が尖る筆致風、ふちがギザギザの透過PNG／SVG |
| 事業内容アイコンの影付きバージョン（影を再現したい場合のみ） | `filter: drop-shadow` | `filter`がテキスト/絵文字に使えないため（3-8参照） | 📷📱相当のアイコンに影を合成した透過PNG（不要なら省略可） |

### 4-3. アイコン・ロゴ以外で、CSSのみで表現されており画像化不要なもの（参考）

以下はSTUDIOのグラデーション背景設定で直接再現できるため、書き出し不要です。
- ヒーロー背景の3色グラデーション（`#05060B → #12162B → #263465 → #649CD3`、斜め160度）
- 事業内容「撮影事業」画像枠の背景グラデーション（`#5E76C7 → #649CD3`、斜め135度）
- 事業内容「SNS運営事業」画像枠の背景グラデーション（`#649CD3 → #2C3B75`、斜め135度）
- CTAセクションの背景グラデーション（`#2C3B75 → #649CD3`、斜め135度）

### 4-4. リンク先ページ（アセットではないが要確認）

ニュース欄からリンクしている以下のページは、STUDIO側でも別ページとして作成が必要です。

| リンク元 | 現在のリンク先ファイル |
|---|---|
| ニュース行1「株式会社Tether Light 設立のお知らせ」 | `news/news-01.html` |
| ニュース行2「コーポレートサイトを公開しました」 | `news/news-02.html` |
| ニュース行3「【サンプル】お知らせのタイトルが入ります」 | `news/news-03.html`（**仮の内容です。差し替えてください**） |
| 「VIEW ALL NEWS」ボタン | `news/index.html`（ニュース一覧ページ） |

---

## 付録: JavaScriptによる動作（`js/script.js`の内容の翻訳）

STUDIOのインタラクション機能で設定する際の参考として、現在のJSが行っている処理を記載します。

1. **ヘッダーの配色切り替え**: ページを10px以上スクロールしたらヘッダーの見た目を切り替える（2-1参照）。STUDIOでは「スクロール位置に応じたインタラクション」機能【要確認・バージョンにより有無が異なる可能性】で設定するか、無い場合はスクロールしても常に同じ配色にする代替案になります。
2. **モバイルメニューの開閉**: ハンバーガーボタンをタップ→ナビが右からスライドイン、ボタンの3本線が✕形に回転。ナビ内のリンクをタップすると自動で閉じる。STUDIOの「モバイルメニュー」機能・要素ごとのクリックインタラクションで再現可能です。
3. **スクロールでのフェードイン表示**: `data-animate`が付いた要素（各見出しやカード等）が画面内に15%入ったタイミングで、下に24pxずれた透明の状態から、元の位置・不透明に0.7秒でアニメーションする（1回のみ発生）。STUDIOの「スクロールで表示」インタラクション機能で近似できます（対象要素ひとつひとつに同じ設定を繰り返し適用する必要があります）。
